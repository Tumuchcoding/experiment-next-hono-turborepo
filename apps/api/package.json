{
  "name": "api",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc && tsc-alias",
    "check:type": "tsc",
    "dev": "bun run --hot src/app.ts",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./api/index.ts --outfile server",
    "start": "node dist/src/app.js"
  },
  "dependencies": {
    "@orpc/contract": "^1.9.4",
    "@orpc/server": "^1.9.4",
    "@outscope/orpc-hono": "^0.2.0",
    "argon2": "^0.44.0",
    "db": "workspace:*",
    "hono": "^4.9.9",
    "reflect-metadata": "^0.2.2",
    "tsc-alias": "^1.8.16",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "tsdown": "^0.15.6",
    "typescript": "catalog:"
  }
}
