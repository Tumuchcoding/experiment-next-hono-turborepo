DATABASE_URL="postgresql://"
NODE_ENV="development"

# The main web origin — where your frontend runs
NEXT_PUBLIC_WEB_URL=https://myapp.com

# Optional backend fallback if you prefer
WEB_ORIGIN=https://myapp.com

# Comma-separated list of allowed origins for CORS
# (include localhost for local development)
CORS_ORIGINS=https://myapp.com,https://admin.myapp.com,http://localhost:3000
