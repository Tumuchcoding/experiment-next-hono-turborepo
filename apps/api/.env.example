# --- Core runtime ---
DATABASE_URL="postgresql://username:password@localhost:5432/database_name" # Required: database connection string
JWT_SECRET="set-a-secure-random-string-here" # Required: secret for signing session JWTs

# --- CORS / Web origins ---
API_ALLOWED_ORIGINS="http://localhost:3001" # Comma-separated list of web origins allowed to call this API, your API site

# --- Optional session cookie tweaks ---
# Leave these blank unless you need to customize cookie domain/behavior
# SESSION_COOKIE_DOMAIN=your-domain.com
# SESSION_COOKIE_SECURE=true
# SESSION_COOKIE_SAMESITE=none
